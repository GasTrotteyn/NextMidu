import { EB_Garamond, Lusitana } from 'next/font/google';

export const garamond = EB_Garamond({ subsets: ['latin'], weight: '700' });
export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
